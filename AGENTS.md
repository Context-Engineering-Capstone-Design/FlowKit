# FlowKit 작업 지침

## 작성 원칙

- `/docs` 문서와 사용자 응답은 알기 쉬운 언어로 작성한다.
- 파일명·변수명은 필요한 경우에만 언급하고, 개발 용어는 이해하기 쉽게 풀어 쓴다.
- 문서는 마일스톤과 체크리스트로 현재 진척을 확인할 수 있게 유지한다.

## 프로젝트 구조와 범위

작업 대상은 `FlowKit/` Git 저장소다. 상위 `캡스톤/` 폴더는 Git 저장소가 아니며, 무관한 자료는 수정하지 않는다.

```text
FlowKit/
├── backend/        FastAPI + PostgreSQL
├── frontend/       React + Vite
├── modeling/       LangChain + Gemini
├── docs/           계획·결과·기타 문서
├── 기획 문서/      요구사항과 백엔드 기능 명세
└── 필요x/          작업 범위 밖 자료
```

사용자는 프론트엔드·백엔드·AI 모델링 영역을 모두 담당한다.

`기획 문서/`는 확정된 설계 근거이므로 임의로 수정하지 않는다. 구현 중 명세와 맞지 않는 점을 발견하면 문서를 바로 바꾸지 말고, 불일치 내용과 이유를 사용자에게 먼저 알린다. 사용자가 요청한 기능이나 수정이 `기획 문서/`에 없으면, 구현하기 전에 명세에 없다는 점을 먼저 알린다. 사용자가 그 내용을 기획에 반영하도록 요청하면 관련 `기획 문서/`도 함께 수정한다.

## 문서 분류

`FlowKit/docs`는 다음 구조로 관리한다.

- `01_plan/`: 앞으로 구현할 마일스톤 계획 문서
- `02_result/`: 완료된 구현의 결과 문서
- `03_others/`: 개발 환경, 설정, 참고 등 계획·결과 외 문서

## 마일스톤 작업 루틴

마일스톤 하나는 아래 순서를 따른다.

1. 구현 전에 `docs/01_plan/`에 계획 문서를 만든다.
2. 계획 범위만 구현하고, 관련 검증을 수행한다.
3. 구현 코드와 필요한 테스트를 Git 커밋한다.
4. 해당 계획 문서에 구현 결과를 반영한다. 기준 커밋 SHA, 구현 항목, 변경 경로, 검증 결과와 남은 제한 사항을 기록한다.
5. 문서를 `docs/02_result/`로 이동한다. 파일명은 `날짜_번호_문서_제목.md` 형식을 유지한다.
6. 문서 이관·갱신 내용을 별도 Git 커밋한다.

사용자는 문서 관리를 위해 각 분류의 문서를 `archive/` 하위로 옮길 수 있다. 즉 `docs/01_plan/archive/`, `docs/02_result/archive/`, `docs/03_others/archive/` 이동은 사용자의 관리 범위다. 에이전트는 사용자의 별도 지시가 없는 한 새 문서를 항상 각 분류의 루트(`docs/01_plan/`, `docs/02_result/`, `docs/03_others/`)에 만든다.

## 문서 기록 원칙

- 계획 문서에는 목표, 범위, 완료 기준, 검증 방법을 기록한다.
- 결과 문서에는 실제 구현된 내용만 기록한다. 미검증 항목과 외부 설정 의존성은 분리해 명시한다.
- 마일스톤·체크리스트의 항목이 실제로 완료되면 즉시 결과 문서에 반영한다.
- 기존 결과 문서와 기타 문서를 삭제하거나 덮어쓰지 않는다. 분류 변경이 필요하면 Git 이동으로 이력을 남긴다.
- 경로에 `필요x`가 포함된 문서와 자료는 기본적으로 작업 컨텍스트에 참조하지 않는다. 사용자가 해당 파일을 명시적으로 지정하거나 참조를 요청한 경우에만 읽는다.

## 커밋 원칙

- 사용자에게 승인된 하나의 작업 단위가 끝나고, 수정 파일이 3개 이상이거나 여러 파일에 걸친 의미 있는 변경이 쌓였으면 다음 작업 전 커밋한다.
- 서로 관련 없는 변경을 한 커밋에 묶지 않는다. 각 커밋은 하나의 목적만 설명할 수 있어야 한다.
- 다른 세션이 동시에 편집할 수 있으므로, 커밋하지 않은 변경을 오래 유지하지 않는다.

## 컴포넌트 역할 주석

새 컴포넌트를 만들 때 `export function` 선언 바로 위에 화면상 역할을 설명하는 한 줄 한글 주석을 작성한다. 코드 줄 단위 설명이 아니라, 컴포넌트가 담당하는 화면·기능을 짧고 쉬운 말로 적는다.

```tsx
// 지원서 작성 화면 전체를 감싸는 공통 틀
export function ApplyShell({ studentId, children }: ApplyShellProps) {
```

## 변경·이슈 기록 원칙

- 버전 이력이나 변경 로그를 실제 화면에 렌더링하지 않는다. 이력은 Git 커밋으로 관리한다.
- 해결 전 문제를 알리는 주석·문서는 남길 수 있다.
- 문제를 해결한 뒤에는 해결 기록성 주석을 코드나 문서에 남기지 않는다. 정상 상태만 유지하고 이력은 Git에서 확인한다.

## 응답 원칙

- 간결하게 결과와 필요한 다음 단계만 전달한다.
- 설명은 숨은 이유, 트레이드오프, 놓치기 쉬운 위험이 있을 때만 짧게 덧붙인다.
- 여러 단계로 이어지거나 시간이 걸리는 작업은 중간 진척 상황을 수시로 보고한다. 지금 무엇을 확인했는지, 무엇을 하고 있는지, 다음에 무엇을 할지를 사용자가 흐름을 놓치지 않도록 이해하기 쉬운 말로 상세히 전달한다.

## 코드 탐색 링크

- 특정 파일이나 줄을 언급할 때는 Markdown 링크를 사용한다.
- 줄 번호는 직접 확인한 경우에만 표기한다. 확실하지 않으면 줄 번호 없이 파일만 언급한다.
