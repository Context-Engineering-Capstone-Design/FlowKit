# 0820_20 통합 대화 노드 구현과 전환

## 목표

`통합 대화 노드 전환 명세`를 실제 서비스에 적용한다. 새 대화 흐름은 모두 Chat 노드로 만들고, 기존 Branch는 한 릴리스 동안 내부 호환 식별자로만 유지한다.

## 범위

1. Chat에 출발 노드·출발 메시지·스냅샷 정보를 추가하고, 기존 Child Branch를 Chat 노드로 일괄 전환한다.
2. 새 분기 API는 현재 노드의 부모 아래에 `분기 N` 노드를 만들고, 출발 메시지까지의 흐름을 복제해 고정한다.
3. 기존 `branchId` 주소는 읽기 호환으로 유지한다. 신규 화면은 대화 노드만 사용한다.
4. 노드 삭제는 하위 노드까지 함께 지우며, 전환 검증·백업 안내·기획 문서를 갱신한다.

## 완료 기준

- [x] 분기 노드가 지정한 부모 위치와 고정된 출발 흐름을 가진다.
- [x] 기존 Child Branch가 메시지·작업 참조를 보존한 Chat 노드가 된다.
- [x] 좌측 트리와 AI 응답 하단 분기 버튼이 통합 노드 API를 사용한다.
- [x] 기존 Branch 조회는 한 릴리스 동안 새 노드 내용을 읽을 수 있다.
- [x] 백엔드·프론트 테스트와 빌드를 통과한다.

## 검증 방법

- 서비스 단위 테스트로 분기 위치, 스냅샷 독립성, 하위 노드 삭제, 기존 Branch 전환을 확인한다.
- API 테스트로 새 노드 생성과 기존 `branchId` 조회 호환을 확인한다.
- 프론트 테스트와 production build로 트리·분기 동작 연결을 확인한다.

## 구현 결과

**기준 커밋:** `e03adc7`

- `POST /api/chats/{chatId}/nodes`가 AI 응답 지점에서 `분기 N` 노드를 만들고, 현재 노드의 부모 아래에 둔다.
- 출발 메시지의 활성 버전을 새 노드 전용 Main 흐름으로 복제한다. 이후 원본 노드의 추가·수정은 AI 입력에 섞이지 않는다.
- Alembic `a82020nodes`가 기존 Child Branch를 새 Chat 노드로 전환한다. 기존 Branch 행과 `branch_id` 참조는 새 노드의 내부 Main 흐름으로 보존한다.
- 기존 `branchId` 상세 조회는 한 릴리스 동안 이동된 노드를 읽는다.
- 좌측 `대화 구조`는 Branch 목록을 제거하고 Chat 노드 트리만 표시한다.

## 변경 경로

- `backend/alembic/versions/a82020_conversation_nodes.py`
- `backend/app/services/conversation_node_migration.py`
- `backend/app/services/chat_service.py`
- `backend/app/routers/side_chat.py`
- `frontend/src/store/chatStore.ts`
- `frontend/src/components/Sidebar.tsx`

## 검증 결과

- `backend/.venv/bin/pytest backend/tests/test_side_chat.py -q` → 25 passed
- `frontend npm test -- --run src/components/Sidebar.test.tsx src/components/MessageBlockItem.test.tsx src/store/chatStore.test.ts` → 77 passed
- `frontend npm run build` → 통과
- 전체 백엔드 테스트는 90% 이후 종료되지 않아 중단했다. 관련 전환 테스트로 범위를 분리해 통과를 확인했다.
- 브라우저 수동 검증은 이번 환경에서 실행하지 않았다.

### QA 후속 확인 (2026-08-21)

`0820_19` QA에서 실제 Chrome으로 처음 확인했다. 코드 변경 없음.

- 마이그레이션 이전부터 있던 기존 대화("물티슈 추천")가 오류 없이 열리고, AI 답변 하단 "여기서 브랜치 생성"으로 "분기 1" 노드가 생성됨을 확인. 좌측 "대화 구조"에 Branch·사이드 채팅 구분 없이 하나의 트리로 표시됐다.
- 분기 생성 뒤 원본 노드("물티슈 추천")에 새 메시지를 추가해도, "분기 1"의 대화 흐름에는 그 메시지가 나타나지 않아 스냅샷 고정이 실제로 지켜짐을 확인했다.
- 새로고침 후에도 트리 구조와 각 노드의 메시지가 정상 유지됨을 확인했다(단, 새로고침 시 열려 있던 탭/선택 노드 자체는 초기 화면으로 돌아간다 — 이 앱의 기존 동작이며 이번 전환과 무관).
- "분기 1" 노드 삭제 시 확인 창이 뜨고 정상 삭제됨을 확인했다. 하위 노드가 딸린 상태에서의 연쇄 삭제까지는 검증하지 못했다.

## 운영 전환 주의사항

- 실제 운영 전환 전 DB 백업과 스테이징 복원·롤백 리허설이 필요하다.
- `a82020nodes`는 Child Branch가 있을 때 데이터를 옮기므로 점검 시간에 한 번만 적용한다.
- 기존 Branch 테이블·호환 코드는 한 릴리스 뒤 제거 대상이다.

## 후속 마이그레이션 적용

**기준 커밋:** `ab32977`

- Project와 대화 노드 계보를 합치는 `a82021merge`를 추가했다.
- 로컬 DB를 `a82021merge (head)`까지 적용했다.
- 백엔드를 재기동한 뒤 `GET /health`, `POST /api/auth/dev`가 모두 `200`으로 응답했다.
