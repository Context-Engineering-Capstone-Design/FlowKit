# 0819_10 대화 삭제

- 상태: 구현 완료
- 기준 커밋: `db899bc5c2498ba94bdb8ffcec7d7568ccd0568f`

## 목표

최근 대화 목록에서 대화를 삭제할 수 있게 한다. 삭제는 되돌릴 수 없으므로 확인을 받고, 지금 보고 있는 대화를 지우면 새 빈 대화로 바꾼다.

## 구현 결과

대화 삭제는 원래 기획에 없어서, 구현 전에 채팅 관리 명세에 `REQ-066`, `FE-CHAT-006`, `BE-CHAT-009`를 추가했다.

- 사이드바 최근 대화 항목 오른쪽에 삭제 버튼을 두었다.
- 공통 확인 창에서 한 번 더 확인한 뒤에만 지운다. 취소를 누르면 아무 것도 바뀌지 않는다.
- `DELETE /api/chats/{chatId}`로 채팅과 브랜치, 메시지, 첨부 파일을 실제로 삭제한다.
- 지금 열린 대화를 지우면 새 빈 대화를 연다. 다른 대화를 지우면 현재 화면은 그대로 둔다.
- 성공은 짧은 알림, 실패는 오류 배너로 알린다. 이미 없거나 권한이 없는 항목은 목록에서 뺀다.

## 변경 경로

- 기획: `기획 문서/요구사항정의서/2.2 채팅 관리.md`, `기획 문서/FE기능명세서/2.2 채팅 관리.md`, `기획 문서/BE기능명세서/2.2 채팅 관리.md`
- 서버: `backend/app/routers/chat.py`, `backend/app/schemas/chat.py`, `backend/app/services/chat_service.py`, `backend/app/services/input_assist_service.py`, `backend/tests/test_chat.py`
- 화면: `frontend/src/components/Sidebar.tsx`, `frontend/src/store/chatStore.ts`, `frontend/src/store/confirmStore.ts`, `frontend/src/components/ConfirmDialog.tsx`, `frontend/src/api/chat.ts`, `frontend/src/types/api.ts`
- 화면 테스트: `frontend/src/components/Sidebar.test.tsx`, `frontend/src/store/chatStore.test.ts`, `frontend/src/components/ConfirmDialog.test.tsx`

## 완료 체크리스트

- [x] 내 대화만 삭제되고, 남의 대화는 거절된다.
- [x] 삭제된 대화는 목록과 상세 조회에서 더 이상 나오지 않는다.
- [x] 확인을 취소하면 아무 것도 지워지지 않는다.
- [x] 지금 보는 대화를 지우면 목록에서 빠지고 새 빈 대화가 열린다.

## 검증 결과

- 백엔드 채팅 테스트: `31 passed`
- 프론트엔드 관련 테스트: `10 passed` (`chatStore`, `ConfirmDialog`, `Sidebar`)

## 남은 제한 사항

- 휴지통·복구는 없다. 지운 대화는 되돌릴 수 없다.
- 여러 대화를 한 번에 지우거나, 브랜치만 따로 지우는 기능은 넣지 않았다.
