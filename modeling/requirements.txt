# LangChain 은 메이저 버전 간 호환이 자주 깨진다. 상한을 두어 팀원마다 다른 버전이
# 깔리는 것을 막는다. 올릴 때는 동작을 확인하고 팀에 공유한 뒤 이 파일을 고친다.
langchain>=1.3,<2
langchain-core>=1.5,<2
langchain-google-genai>=4.3,<5
langgraph>=1.2,<2
pydantic>=2.7,<3
# PDF 첨부에서 글자를 뽑는다.
pypdf>=6.1,<7
pytest>=8.0.0
